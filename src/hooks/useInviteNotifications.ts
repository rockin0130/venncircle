/**
 * Placeholder hook — in-app toast & Web Notification for group invites
 * were removed per product decision. Invites are now surfaced only via:
 *   - Chat list invite card
 *   - Notification center badge
 *
 * Kept as a no-op export so existing imports continue to work.
 */
export function useInviteNotifications() {
  // intentionally empty
}
